worker_processes 4;
 
events {
     worker_connections 1024;
     }
 
http {
    sendfile on;
 
    upstream auth_api {
        server auth_api:8080;
    }
 
    server {
        listen 80;

        location / {
            proxy_pass http://auth_api/;
        }
    }
}